name: calendar_event_extractor
description: Extract structured calendar event(s) from user input.
prompt: |
  Today is {{today}}.
  User input: "{{input}}"
  Extract event details as JSON with keys: title, date, time, location, notes.
  Respond with JSON only.
vars:
  - input
  - today
